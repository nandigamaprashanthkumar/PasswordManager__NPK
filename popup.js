// popup.js

function savePassword() {
    // Get the values of username and password from the form
    const username = document.getElementById('username').value;
    const password = document.getElementById('password').value;

    // Perform any additional logic (e.g., validate inputs, send data to the server)

    // Log the values (for demonstration purposes)
    console.log('Username:', username);
    console.log('Password:', password);

    // Add logic to send the data to your Flask app or perform other actions
    // ...
}
